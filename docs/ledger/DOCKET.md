# DOCKET — current & future work

Everything requested, planned, or ideated that is NOT yet shipped. One line per item: status, date logged, who asked, what. Move items to SHIPPED.md when done. Update this file in the same commit as the work itself — worktree/subagent sessions included. Statuses: IDEA · QUEUED · IN PROGRESS · BLOCKED · STAGING (awaiting prod).

## Staging — awaiting prod go-ahead

(address paste-to-fill SHIPPED TO PROD 7/27 in promote e6c15f8 — verified live on prod with a multiline "Nebraska"+USA paste; see SHIPPED)

(price-book NULL-tier dedupe SHIPPED TO PROD 7/27 in promote e6c15f8 — verified live: 609→459 rows, 0 dup groups, 17/17 keepers = 1-20 price, SRA $11,200→$1,800, duplicate insert 409-blocked; see SHIPPED)
- [QUEUED] 2026-07-27 · (sweep follow-up) · **SF importer pricing guard**: SalesforceImport plain-inserts price_book_entries deduped only by sf_id (SalesforceImport.tsx ~4342); after the NULLS-NOT-DISTINCT constraint a PricebookEntry-CSV re-import fails loudly per duplicate row (23505 in the failed-rows panel) instead of silently recreating debris — intended, but the importer deserves a real upsert or a hard "pricing imports are closed" guard before anyone re-imports pricing. Also worth folding in: derive fte_range from the SF product-name prefix when the book name lacks one.

(task-digest send guard + restored GH backup schedule SHIPPED TO PROD 7/27 in promote e6c15f8 — task_digest_log live; see SHIPPED)

(Automation badge SHIPPED TO PROD 7/27 in promote e6c15f8 — verified live: 67 signature tasks backfilled + badged; see SHIPPED)

(cron stagger + set-based status sweep SHIPPED TO PROD 7/27 in promote e6c15f8 — verified live: all 6 dailies on distinct slots, customer_status distribution byte-identical before/after; see SHIPPED)

(the Activities date-filter fix shipped to PROD in promote 75eaaf0, same day — verified live with Summer's exact range; see SHIPPED)

- [IN PROGRESS — only Camp Lowell left] 2026-07-27 (v2; batches 1-3 ALL EXECUTED same day: 11 delete-safe + 4 Margaret-confirmed + the 5 Closed-Won = 20 deals, $159,423 phantom/double-counted removed; root fix + tag PROMOTED to prod a1c3514; remaining: Camp Lowell $1,012.50 awaits Rachel; Keena $4,400 kept, real renewal not a duplicate) · **Duplicate auto-renewals, FINAL split via Nathan's retro test** ("would the fixed automation have created it?"). All 91 children machine-made beyond doubt: 75 share one microsecond created_at (2026-07-20T23:10:38.734559Z batch), rest sit exactly on 09:00 cron / manual-run stamps, all created_by_automation. **DELETE, no consultation needed (11 = $105,698):** twin/successor existed BEFORE the child was generated + covers every parent product — GBMC $44,654 (manual twin 4/22 vs child 7/20; keep Margaret's manual, optionally copy the child's correct contract dates onto it), Keena June-chain $13,200 (succ created 2/28), Summit $9,700 (succ 1/21), Western NC $8,740, Speare $8,100 + $3,500 (succ 3/4), Colorado Allergy $7,232, Galvanic $5,060 (succ 4/14), Southland $4,600 (succ 4/5), Listen Hear $456 (twin 3/28), Hearing Doctors NJ $456 (twin closed LOST — renewal declined). **ASK (6 = $39,069):** fixed automation would STILL have created these (partial product coverage or one-successor-many-chains ambiguity) — Keena Oct-chain $13,200 + Keena $4,400 (one $13,200 successor can't renew three chains totaling $30,800; consolidation? → Margaret), Universal Primary Care $7,300 (BNVA uncovered), Gunnison $6,906.50 (Remote Services uncovered; succ is SRA-only $3,192 downsell?), Neurology Center $6,250 (P+P dropped, same $), Camp Lowell $1,012.50 (succ is "SP SRA" 3-yr $499 — likely the same product renamed → Rachel, she closed it). **LEAVE (74):** incl. the SAFER/Pullman near-matches — successor doesn't cover the child's product at all, so the child is the real renewal. Owner note: Brayden Frost owns many children but is NOT with the company — his are effectively unowned; route via claim panel. The 5 already-Closed-Won ($20,068.50) stay gated on Margaret's reply (note sent 7/27; Blue Mountain's successor swapped Custom Service→SAFER at identical $7,647.50, so it's confirm-with-her, not auto-delete).

- [QUEUED] 2026-07-27 · Nathan/Margaret · **DUPLICATE AUTO-RENEWALS — verified cleanup list + the root fix.** Margaret confirmed she has been closing duplicates out (7/27: "I closed a few out on Friday that I had previously closed as well"). ROOT CAUSE: the generator's only dedup is "does this parent already have a child linked to it (`renewal_from_opportunity_id`) or a suppression row?" The April SF-imported renewals are standalone deals with NO parent link, so every contract whose renewal was already handled that way looks un-renewed and gets a second, automated renewal. **(A) 5 already Closed Won = $20,068.50 double-counted** — Blue Mountain Hospital (Utah) $7,647.50, Hana Health $5,745, Ko-Kwel $4,900, Rheumatology & Osteoporosis $1,296, Beautiful You Dermatology $480. All 5 verified by contract chain (parent = 2025→2026 term; a same-product successor was already Closed Won weeks earlier). Nathan deletes these — RLS blocks sales/renewals roles from deleting closed_won, admin only. **(B) 9 open, verified duplicates = $82,798** — Galvanic $5,060, Southland Neurologic $4,600, Speare Memorial $8,100 + $3,500, Western NC $8,740, Colorado Allergy $7,232 (successor at $9,040, price rise), GBMC $44,654 + Listen Hear $456 (both have a live OPEN manual twin, not a closed one), Hearing Doctors of NJ $456 (its manual twin was closed LOST 7/27). **(C) 7 NEED HUMAN EYES = $60,956.50** — Keena Healthcare ×3 ($13,200/$13,200/$4,400: genuine multi-contract account running many parallel Custom Service deals, 3 children matched to 1 successor, at most one is a dupe — my matcher over-fired here), Summit Medical $9,700 (account already carries two overlapping 2026 contracts), Gunnison Valley $6,906.50 (successor is SRA-only $3,192 vs parent SRA+Remote — looks like a downsell), The Neurology Center $6,250 (successor dropped P+P), Universal Primary Care $7,300 (product change SRA|BNVA → SRA|Remote). **(D) 75 clean** — real renewals, leave alone. Deleting a child writes a `renewal_suppressions` row keyed on the PARENT (trg_opportunities_renewal_suppression, 20260612000001), so deletes are permanent and the successor contract still generates its own renewal next cycle. **THE FIX (Nathan's priority):** before creating, also skip when another deal on the account — Closed Won OR open — has a contract period covering the parent's anniversary with overlapping products, and record the skip reason. Without it, deleting today's batch just yields a fresh crop as more SF-imported renewals hit their anniversaries. Verification method that worked: match on the CONTRACT CHAIN (parent contract_end_date as anchor) + line-item product overlap, not deal names; name-only matching produced false pairs (a $4,440 SRA renewal matched a $450 GE Training contract).

(the generator covering-deal dedup shipped to PROD in promote a1c3514, 7/27 — migrations 20260727120000+130000 applied, preview verified on prod data; see SHIPPED)

(the Auto-renewal tag shipped to PROD in promote a1c3514, 7/27 — verified live, 76 of 107 renewal cards tagged; see SHIPPED)

(the 7/24 Requests dialog cleanup shipped to PROD in promote a1c3514, 7/27 — see SHIPPED)


(The Azure SWA upload outage RESOLVED ~15:50 PT 7/21 — Nathan's empty-commit pipe test afdc08f went green and shipped the two stuck approved frontends; see SHIPPED. The 7/21 afternoon batch — ghost-stage migration, partner banner, claim tool, phone auto-fill, FTE gate fix — DID ship to PROD in merge aaec792; this line previously claimed otherwise and was stale. Confirmed live 2026-07-27 from prod data: the ghost-stage re-stage row (changed_by = null, 7/21 22:53 UTC) is present on prod's opportunity_stage_history.)

(The 2026-07-15 batch — Joe Lead Source, Rachel Assessor, Summer email fix — shipped to prod in 5bc05df; the 7/10 Lead-Source-required + 7/11 ClickUp items were already on prod via 9d6b6a7. See SHIPPED.md.)

## Queued / requested

- [BLOCKED — awaiting Summer's reply, then a Summer/Rachel decision] 2026-07-27 · Summer (CRM request "Partner Tab on Account confusing", low) · **Partner tab cleanup.** Her ask: delete the top card (Partnership Status / Partner Type / Relationship Notes), move the member-accounts list up into that space, plainer language, and drop the partner-vs-member distinction in the Add Partner dialog ("arrows that I really am not sure about"). NOT started — Nathan pausing it because the current layout IS her own 2026-07-17 request: that batch retired the "Partner Information" section and nested those three fields under the Partner checkbox + a detail-tab card (migration 20260717000003, PROD 8bbec0f). **The blocker: Partner Type is RACHEL's field** (2026-07-07, `accounts.partner_type` + picklist_options, REQUIRED-when-partner via the AccountForm.onSubmit rule) — removing the card hides a required field, so it's a two-teammate decision for Nathan to broker, not mine. **The distinction is NOT droppable either:** `account_partners` holds 512 live relationship rows on prod (oldest 2026-04-25, newest 2026-07-27 — actively used), and partner_account_id vs member_account_id is the direction of referral; the fix there is plain-English wording in the Add Partner dialog, not removing the concept. Data check 2026-07-27: only 7 accounts have partnership_status/relationship_notes set and 7 have partner_type — the two Summer-owned fields are ~0% populated, so they're the cheap ones to drop. **Proposed shape when unblocked:** (1) remove the card, relationship list moves up; (2) relocate the 3 fields to the edit form under the Partner checkbox (keeps Rachel's required field reachable) — or keep ONLY Partner Type visible as a small badge if Rachel wants it on the page; (3) rewrite tab + dialog copy in plain terms ("Referred by this partner" / full sentences instead of arrows). Nathan drafting a reply to Summer explaining the July-17 history first.


- [QUEUED] 2026-07-22 · (Campaigns audit — deferred lower-severity findings) · **RE-VERIFIED 2026-07-27 against the code.** RESOLVED since logging: **(a) spawnCampaignTasks wholesale-chunk-failure is FIXED** — playbook-smartlead now pre-checks existing (enrollment, step) pairs and chunks PER-ENROLLMENT, so a failed chunk leaves tasks_spawned_at null and simply retries next run (no more unrelated enrollments starved). STILL OPEN, all confirmed present today: **(b)** playbook/csv.ts HEADER_MAP line 42 maps a single `name` column → `first_name` outright, so "Jane Smith" lands as the first name; add a space-split or a mapping warning. **(c)** campaign-scheduling.ts ptUtcOffsetHours() still buckets DST by month, so task due TIMES are off an hour for ~1 week before each US transition (never the wrong day; cosmetic). **(d)** useCampaignEventStats still ships every campaign_events row to tally 4 buckets — lazy + index-supported so cheap today; convert to a count/group-by RPC before high-volume campaigns. **(e)** reconcileCampaignLeads still does sequential single-row writes (capped 25 campaigns/run; batch before volume grows). **(f)** no-double-enroll TOCTOU — two concurrent launches can enroll the same email; needs an advisory lock (a unique index is ruled out: enrollment_overrides deliberately allows double-enroll). **(g)** LOW: v_campaigns_daily_sweep_schedule_status still granted to `authenticated` (matches the existing cron-view precedent).

- [STAGING] 2026-07-27 · (Prod RAM / performance — LINE COMPLETE, nothing left) · **(a) cron stagger + (b) status-sweep rewrite SHIPPED TO PROD 7/27 (e6c15f8). (d) Home KPI amount-sum moved SERVER-SIDE — on staging, commit 0d56c01** (migration 20260727210000): fetchAllOppAmounts paged every matching opportunity amount to the browser (1,000/request, up to 50,000) on every Home load for three tiles; replaced by one parameterized SUM RPC (SECURITY INVOKER so caller RLS still applies). Verified on staging against the old paged method: My Pipeline exact match; Team Pipeline 156 rows paged → 0, and the old JS sum was 816593.0299999999 vs the RPC's exact 816593.03 (server-side numeric is strictly MORE accurate, same displayed currency); quarter tile match. kpiRegistryErrors guard extended to the RPC path (295 green). **(c) CLOSED 7/27 by Nathan ("fair, remove from docket") — measured, then deliberately not fixed:** 5-run medians on staging showed an 8-email `.in()` on v_marketing_suppression at 140ms vs 110ms for 1,000 rows, so the filter does NOT push into the CTE (all 39,039 rows computed per call). Left as-is: 140ms is immaterial, and materializing or re-implementing the EXISTS branches would fork the do-not-email COMPLIANCE invariant into two places. If suppression volume ever makes this matter, materialize the ONE view — never duplicate its logic.

(STALE, removed 2026-07-27: verified on prod — 0 campaigns created in the last 30 days, so the migration-touched timestamps have aged out of the 30-day window and the strip no longer over-counts. If genuinely-launched campaigns resume and the count looks wrong again, filter on origin='pulse'.)

- [QUEUED] 2026-07-22 · Nathan · **Campaigns PUBLIC LAUNCH checklist (when Jordan's wording is in + Nathan has personally tested):** (1) put Jordan M's real copy into the 8-Touch + Warming presets (template editor or seed-update migration); (2) flip ACTIVE_ANNOUNCEMENT in src/components/AnnouncementBanner.tsx (fresh id, ctaRoute /playbook); (3) remove the admin gates at the three "Rep rollout flip point" marks (App.tsx route, Sidebar adminItems, ContactsList right-click) — rep-facing RLS + edge-fn ownership gates are already live; (4) consider clamping launch leads_per_day to the chosen inbox's daily limit (docketed separately). Launch-day order: copy → test → flips together. Also decide then: the two Playbook GH workflows disabled since 6/26 (smartlead-sync, weekly-ideas) — likely superseded by the Campaigns engine; re-enable only if Playbook stats still wanted.

- [QUEUED] 2026-07-22 · (Campaigns Phase 5 finding) · **Launch default leads_per_day (25) can exceed a sending inbox's daily limit** (live example: news@accessmedcurity.com is 15/day, a launch defaulting to 25/day shows "Room for ~0 more people/day" in the new Sending-inboxes panel). Fix: clamp leads_per_day to the chosen inbox's warmup/daily limit at launch, or lower the default and let the panel guide. Not urgent — the panel surfaces it honestly today.

- [QUEUED] 2026-07-22 · (Phase-3 cosmetic follow-ups) · Campaigns detail sheet: (a) First-send column renders a day early (date-only first_send_at through a local-time formatter — format date-only values in UTC); (b) Step column stays "Not sent yet" until events carry sequence numbers (could infer email-1-sent from campaign metrics). Also: delete the "LIVE TEST — Phase 2 webhooks" campaign from the tracker once Nathan has looked at it. (The sweep-replies→campaign_events gap from the Phase-2 live test SHIPPED in 6ace3d0 — see SHIPPED 7/22 Phase 3.)

(Summer's Automation-badge item BUILT 7/27 — see the STAGING entry above. Still optional if ever wanted: a standing cross-sell revenue report by business_type — Summer said not needed 7/22.)

(DONE, removed 2026-07-27: Nathan performed the Medium (4GB) upgrade — already recorded as the OOM fix in the perf line above. Line was never closed.)

(MOOT, removed 2026-07-27: the 7/22 crash-window logs were never going to be worth chasing 5 days on — mechanism (OOM on the 2GB tier) was already certain from the memory graphs, capacity is fixed, and the 7/27 prod cron check confirmed the scheduler healthy. The cron-job-6 cadence sanity-check inside it was ALSO answered 7/27: meddy-stale-agents at every-minute is intended and cheap.)

(the ENTIRE 7/21 afternoon batch — ghost-stage fix, partner AI banner, claim tool, FTE gate fix, phone auto-fill — shipped to PROD in merge aaec792 ~16:10 PT; see SHIPPED)

(the 7/22 staging-legacy-jobs item is RESOLVED same day — jobs removed, root cause = angle-bracket-wrapped key from a 7/15 re-paste, NOT a key rotation; see SHIPPED 7/22. Two follow-ups spun out below.)

- [IN PROGRESS] 2026-07-22 · Nathan · **Campaigns overhaul (THE big project): make email campaigns the easiest, most reliable tool in Pulse.** Full plan + 2026-07-22 decisions in docs/campaigns/campaigns-plan.md (kickoff addendum). Phase order: (1) trust-the-basics — unify the two campaign data models, close the CSV/paste suppression hole, wire "Use this template" to launch, beautiful Ongoing+last-30d tracker with pause/stop from Pulse; (2) engine — per-person enrollments, Smartlead webhooks, call/LinkedIn tasks in Up Next+Nexus, auto-stop on reply, stop/edit mid-flight; (3) easy path — right-click → solo campaign, My Campaigns (admins see all); (4) analytics + in-tab reply feed; (5) AI insights + deep personalization; (6) scale (multi-inbox, warmup, timezones, rep access). Admin-only + staging-only until proven. Nathan getting Jordan M's 8-Touch/Warming copy.

(the prod cron check RAN 2026-07-27, Nathan pasting read-only SQL: CLEAN — all 11 jobs recognized/active, zero bracket bugs, no legacy dead jobs, no 401s. The 38 NULL http responses = email_sync's every-10-min calls exceeding pg_net's 5s wait while the function completes fine (60 emails logged same day, newest minutes before the check). meddy-stale-agents' every-minute cadence sighted and fine (cheap SQL sweep). Optional cosmetic someday: raise timeout_milliseconds on the email_sync cron call so the NULL noise disappears from net._http_response. See SHIPPED.)
(the disabled Playbook GH workflows item folded into the Campaigns public-launch checklist 7/27 — decide at launch: superseded by the Campaigns pipelines, or re-enable)




(RESOLVED 2026-07-27, removed: nothing was reopened; root cause was duplicate auto-renewals — 20 deleted, generator fixed, Auto-renewal tag shipped. Full record in SHIPPED + docs/cleanup/2026-07-27-duplicate-renewal-deletions.md.)
- [QUEUED] 2026-07-21 · (ghost-stage follow-up — items (a) and (b) RESOLVED 7/27: (a) shipped as the Auto-renewal badge, (b) the duplicate-children review became the 7/27 cleanup, 20 deleted) · remaining: (c) surface legacy stages in list filters or a data-health count so ghost-stage rows can never hide again (deliberately left out of the DB-only fix).

(renewal-opp ownership RESOLVED at 2026-07-21 standup, Jordan S. via Nathan: assessor-of-the-previous-deal owns the renewal when listed, else the previous deal's owner — which is EXACTLY the generator's existing coalesce(assigned_assessor_id, owner_user_id) rule, so no code or data change was needed. Verified on the 105 generated opps: 103 owner-inherited / 2 assessor-inherited, incl. all 5 of Molly's — she was the sales rep on those originals, so they're hers to manage or reassign per the decision. Signature auto-tasks removed + archived separately, see SHIPPED 7/21.)

- [QUEUED] 2026-07-21 · (7/21 perf/reliability fleet — confirmed-low findings deferred with eyes open) · **RE-VERIFIED 2026-07-27.** STILL OPEN: **(a)** the meddy agent-ding subscription on `meddy_messages` (useNotificationToasts.ts ~line 232) has NO postgres_changes filter — every INSERT company-wide reaches every open staff tab, each triggering a DB round-trip; fix = a broadcast channel scoped to joined conversations, or a client-side joined-conversation Set gate. (The sibling `notifications` subscription IS correctly filtered by user_id — only the meddy one leaks.) **(c)** RLS InitPlan wraps landed for exactly 5 tables (accounts, activities, contacts, leads, opportunities); the aux tables (opportunity_products, stage_history, products, user_profiles, audit_logs, …) are still unwrapped. **(e)** OpportunityContacts still fires one activities query per stakeholder (N+1). **(f)** last-activity views still aggregate the whole activities table on unscoped sort paths. **(g)** RenewalsQueue still unvirtualized (zero virtualization in the file) — double table + ~13 full-list filter passes per totals memo. **(i)** duplicate dashboard-metrics cache keys (TeamDashboard vs the standard report). **(j)** HomePage chunk prefetch fires for deep-linkers who never visit Home (deliberate boot-waterfall tradeoff). RESOLVED/MOOT: **(d)** AccountDetail no longer uses select('*') on its child lists; **(b)** stale-chunk recovery on feature-level dynamic imports — superseded by the boot-recovery work; **(h)** LeadDetail's "Import not found" masking — moot, the lead type was retired 2026-07-20.

(Joe's Sept-renewals item RESOLVED on prod 2026-07-20 evening — see SHIPPED; the follow-up 113/97 past-due reconciliation ALSO resolved same night, past-due = 0 — see SHIPPED)

- [IDEA] 2026-07-20, downgraded 2026-07-27 · **155 pre-audit-horizon renewal parents (2024-vintage, anchors 6-18 months past).** The automation never touches them (outside every window) and the 7/27 covering-deal dedup protects the future, so nothing is at risk. Optional someday: an anchor-based hygiene sweep to write them suppression reasons like the 97, plus giving v_renewal_audit an anchor horizon so multi-year contracts stay visible. No conversation or decision needed to leave as history. NOTE from the 7/20 sweep, still true for any future sweep: use the GENERATOR's anchor (coalesce(contract_end, signed+len, close+len)) for past-due predicates, not the audit's close+12mo.

(the 2026-07-17 "Reports based on tags" ask shipped 2026-07-20 inside the unified Reports+Lists build — Builder Tags filter; see SHIPPED)


(the 2026-07-17/20 Safari loading item moved to Staging above — shipped 2026-07-20 in deeaae3)
(STALE, removed 2026-07-27: this was my own relay reminder — every item it listed (Rural Hospital industry option, Notes on Contacts, delete/merge duplicate accounts, Active tab) SHIPPED TO PROD the same day in 8bbec0f. Never cleared. See SHIPPED 2026-07-17.)
(task-digest dedup guard BUILT 7/27 — see the STAGING entry above)
(the price-book NULL-tier item BUILT 7/27 — three-agent sweep upgraded it from "cosmetic dedupe" to the real fix; see STAGING entry. NOTE the docket line's old "pricing correct, cosmetic only" was WRONG: the default-book fallback served arbitrary tier prices — prod-verified SRA $11,200, SAT $15,000, SRA-Remote $0 — to size-unknown deals.)
(the lead-type retirement SHIPPED TO PROD 2026-07-20 evening in merge 750527e — prod had zero pending leads, no straggler sweep needed; see SHIPPED. Still deferred beyond retirement: send-time suppression enforcement, the standing audit item · plan doc closed: docs/imports-tab-plan.md)

- [QUEUED] 2026-07-20 (pre-workday sweep, 36-agent audit) · **RE-VERIFIED 2026-07-27.** STILL OPEN: **(c)** the admin Renewal card still renders both pull-back knobs (pullback_days_auto_renew / _signature_required, 6 references in RenewalAutomationCard.tsx) but the live generator ignores them entirely — dead controls that imply configurability; either wire them or remove them. **(d)** renewal preview cosmetics: suppressed parents get a generic "Already has renewal" reason and the v_renewal_audit past_due note still promises the old wide lookback (bundle into the next audit-view re-emit). **(g)** task reminder/digest emails still deep-link `/leads/{id}` for unconverted-lead tasks (task-digest/index.ts:104, task-reminders/index.ts:156,294) — works for admins via the forwarder, sales users bounce; leads are retired so these should point at /imports or be dropped. **(b)** read_only role still sees Lists write buttons (RLS blocks the writes; cosmetic gate missing). **(e)** ImportsPen search still has no debounce (admin-only page). **(f)** the two April-era prod lists (Nebraska Leads, FQHC) are empty husks post-retirement — worth telling Summer; an INDUSTRY smart-list rule doesn't exist, so FQHC rebuilds via Report Builder → Save as list. **(a)** email_dup_status not pen-aware (avoided/pending imports misclassified in dedup surfaces — verifier-downgraded LOW).
(SUPERSEDED block removed 2026-07-27 — the lead-type retirement completed 2026-07-20; its history lives in SHIPPED and docs/imports-tab-plan.md.)
(the two 2026-06-29 Summer items — surface derived Client/Prospect/Former label + required closed-lost prompt — shipped as part of the account-status restructure, prod 2026-07-15 e01ed6a; see SHIPPED.md)
- [QUEUED] 2026-06 · Brayden · Guided account-creation / require-at-the-right-moment flow (partially delivered via the 2026-07-08 Closed Won gate; the guided create popup itself still open)
- [QUEUED] 2026-06-24 · (audit) · Deferred audit items: PandaDoc webhook, suppression enforcement (+ over-suppression decision), leadership-numbers fixes · docs/audit/
- [QUEUED — needs Molly, 1 question] 2026-06-10 · Molly · **On-Site Fee: the exactly-250 boundary + the never-built phase-2 auto-add.** VERIFIED 2026-07-27, both still open. The product itself is live and used on 3 prod deals; its pricing was separately FIXED on 2026-06-10 (d123512 / migration 20260610000003, 'price every FTE range in every active book') — that fix is likely what got remembered as 'the Molly thing is done'. What remains: (1) `employeesToFteRange` (formatters.ts:296) buckets `<= 250` into '101-250', so a company with EXACTLY 250 employees prices at $500; Molly's stated rule was '250 or more = $1,000'. One-employee edge, one-line change, but it's her pricing call. (2) Auto-add the fee when an on-site SRA is selected — grep of src/features/opportunities/ finds no on-site-fee logic, so phase 2 was never started.
(duplicate of the task-digest guard line — BUILT 7/27, see STAGING entry)

## Ideas / someday

- [IDEA] 2026-07-17 · Nathan · AI smart lists: Ask AI assembles/curates a call list from natural language ("every non-customer in Washington", etc.) — lists over reports because membership can be edited without touching contact data. Builds on the tags + lists + reports infrastructure; ties into the CRM AI layer idea below. Nathan: "if it's somewhat ai powered in the future and extra easy to use that could be super cool."

- [IDEA] 2026-07-11 · Nathan · Account Snake mini-game (classic snake; eat accounts to grow your book of business; hide on the Accounts nav label) — Deal Merger won the pick, Nathan said snake "maybe another time"
- [IDEA] 2026-07-08 · Molly · FTE autofill reverse direction (opportunity → account) — one-way account→opp shipped; Molly's original ask was bidirectional
- [IDEA] 2026-07 · Nathan · Pulse Arcade hub (games list, profiles, records, daily events) — revisit at 6+ games (raised from 3+ on 2026-07-11; currently 3: Runner, MeddySweeper, Deal Merger)
- [IDEA] 2026-06 · Nathan · CRM AI layer (describe-a-report, AI nav/search) — big project, deferred
- [IDEA] 2026-07-22 · Nathan · Ask AI → campaign: describe it in plain English ("find my prospects in the Southwest, start them on a 2-email campaign referencing our customer there") and AI sets the whole thing up. Future layer on the Campaigns overhaul (build campaigns with clean internal APIs so AI can drive them later); ties into CRM AI layer + AI smart lists ideas above.
- [IDEA] 2026-07-08 · (review note) · Nexus called-by filter caps at 250 contact ids per caller — sanity-check under real volume post-adoption

## Watch / verify later

- [WATCH] 2026-07-07 · (perf batch) · Meddy widget 1h cache: keep meddy-chat edge fn backward-compatible ≥1h after widget-affecting deploys
- [WATCH] 2026-07-05 · Nathan · Nexus dashboard: Home stays default tab until Nathan approves switching (Cold Call ICP question superseded 2026-07-15 — Summer's curated call lists are the source now, picker shipped in the restructure)
